global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: spring-boot-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - app:8080

  # Future exporter scrape targets:
  # - job_name: mysql
  #   static_configs:
  #     - targets:
  #         - mysql-exporter:9104
  # - job_name: redis
  #   static_configs:
  #     - targets:
  #         - redis-exporter:9121
